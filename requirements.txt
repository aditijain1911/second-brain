









# Make sure your venv is active — you should see (venv) at the start of the line
# If you don't see it, run this:
cd D:\quickgpt\second-brain
venv\Scripts\activate

# Now install everything at once
pip install qdrant-client sentence-transformers fastapi uvicorn sqlmodel anthropic python-dotenv langchain







python -m pip install fastapi uvicorn sqlmodel qdrant-client sentence-transformers groq python-dotenv langchain-text-splitters



Restart your backend:

uvicorn main:app --reload
